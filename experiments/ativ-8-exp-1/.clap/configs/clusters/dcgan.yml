setups:
  default:
    roles:
    - name: setup

    actions:
    - role: setup
      action: clone-dcgan

clusters:
  dcgan:
    nodes:
      main:
        type: dcgan-type-a
        count: 1
        setups:
        - default
